VSCode에서 git으로 연동하는 연습을 하였음

내용은 fetch에 대한 것이며, 프론트 엔드에서 외부 api로부터 데이터를 받을 수 있는 코드인 것 같음
참고로 api는 전통적으로 엘든링 api인

https://eldenring.fanapis.com/api/bosses?limit=20&page=3

이거를 사용.

node.js에 대한 공부도 하고 실습까지 다 몰아넣었음
node.js는 js를 크롬 브라우저의 V8 엔진 같은 것인데, 웹 이외의 환경에서 js를 실행시킬 수 있는 외부엔진기계 같은 프로그램.

npm이라는 기능이 있는데, node.js를 받으면 같이 받아지며, 이는 js 코드를 작성하면서
외부 모듈을 관리하는 기능임. 즉 node.js로 js를 관리하면서, 외부 모듈을 관리하는 기능을 가진 node.js의 또다른 모듈인 셈

npm init을 통해서 package.json 파일을 생성하며, 여기에 기본적인 프로젝트의 설정들이 생긴다
예를 들어 package.json 파일의 scripts 부분은,
"start": "node ex2.js", 와 같이 node.js 명령어를 달리 사용하는 명세를 작성할 수 있다.
즉 "node ex2.js" 라는 명령어는 이제부터 "npm start" 라는 명령어로 사용할 수 있게됨

한편 npm으로 관리하는 외부 js모듈들 역시 npm init으로 생성되는 package.json에 명세된다.
